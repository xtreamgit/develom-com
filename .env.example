# Payload
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Vercel Postgres
POSTGRES_URL=postgres://...

# Vercel Blob Storage
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# Email (SMTP — optional, falls back to stream transport)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@develom.com
